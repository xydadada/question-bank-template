# 已知限制

这里记录的是模板会主动停止并保留资料的边界，不是静默降级项。

- 当前 `state.db` 仍以内容 SHA-256 作为旧版文件主键。两个仍存在、路径不同但
  字节完全相同的源文件不能安全地并行保存独立状态，因此程序会停止该资料组并
  要求只保留一份；它不会让两份文件共用批次、Markdown 或删除状态。
- 云端 MinerU、MiMo、真实 WeKnora 和 ChatGPT Workspace 需要下载者自己的账号、
  密钥和内容。CI 只能验证本地逻辑和失败边界，不能替代一次可丢弃样本的真实
  端到端确认。
- 永久删除使用摘要、目录范围、显式确认和审计记录保护，但文件系统删除与
  SQLite 提交无法组成真正的跨系统原子事务。异常断电后，程序只会根据已提交
  的删除意图和哈希一致的保留 Markdown 收敛审计；无法确认的记录会明确保留为
  失败状态，不要手工伪造完成记录。
- WeKnora v0.7.1 上游 Compose 为多个服务固定了 `container_name`。模板会为
  每个克隆生成稳定且不同的 Compose 项目名，防止停止脚本误认领其他克隆；
  但同一 Docker daemon 上同时启动第二份 WeKnora 仍会因上游固定容器名冲突而失败。
  这是拒绝启动，不是共用第一份的数据。
- WeKnora 已发布 v0.7.2，但它包含约 200 个提交，并改动 CLI、MCP、分块、检索和
  Compose。模板仍固定在已完成兼容性审查的 v0.7.1，而不是自动跟随最新版。升级需要
  重新核对 CLI JSON 契约、三层检索、MCP 工具面和停止脚本；不要只改版本字符串。

若这些边界影响使用，优先提交可复现的最小样本；不要绕过安全检查。
